version: "3.2"

services:
  backup:
    image: bob5ec/backup
    volumes: 
     - ./root:/root
     - ./data:/data
     - ./data/home:/home
    links: 
     - test-sshd
  test-sshd:
    image: panubo/sshd
    environment:
     - SSH_USERS=test:1000:1000
    expose:
     - "22"
    volumes:
     - ./root/.ssh:/etc/authorized_keys
