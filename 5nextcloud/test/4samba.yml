version: "3.3"

services:
  samba:
    image: "bob5ec/samba:prod"
    ports:
     - "445:445"
    container_name: samba
    volumes: 
     - ./samba/test/secrets:/secrets
     - ./samba/test/data:/data
     - ./samba/test/data2:/data2
     - ./samba/test/data/home:/home
