version: "3.3"

services:
  samba:
    volumes: 
     - ./secrets:/secrets
     - ./data:/data
     - ./data2:/data2
     - ./data/home:/home
  samba-client:
    image: bob5ec/samba-client
    links:
     - samba
